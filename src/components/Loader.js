function Loader() {
  return (
    <div
      style={{
        padding: "20px",
        fontSize: "20px",
        fontWeight: "bold",
      }}
    >
      Loading transactions...
    </div>
  );
}

export default Loader;